import 'package:flutter/material.dart';

class FooterName extends StatelessWidget {
  const FooterName({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Birang Pawala");
  }
}